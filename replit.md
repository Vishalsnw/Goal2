# Project Overview

This is a minimal project imported from GitHub that originally only contained a LICENSE file.

## Current State

A simple static website with a welcome page has been set up to make the project runnable.

## Project Structure

- `index.html` - Main welcome page
- `server.py` - Simple Python HTTP server for development
- `LICENSE` - MIT License

## Development

The project runs on port 5000 using a Python HTTP server.

## Deployment

Configured as a static site deployment serving files from the root directory.
